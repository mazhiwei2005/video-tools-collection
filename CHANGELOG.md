# 更新日志 / Changelog

## [2.2.0] - 2026-06-08

### 🆕 新增功能

#### 画中画模式大幅升级
- **模板支持视频文件** — 画中画模板不再局限于图片，可直接选择视频作为背景
  - 视频模板自动循环播放到正片时长
  - 智能预裁剪方案，避免NVENC时长异常
- **PR风格效果控件面板** — 区域框选升级为专业级操作界面
  - 左侧预览画布支持滚轮缩放、拖拽平移
  - 右侧数字输入面板：X/Y坐标、W/H尺寸精确控制
  - 预设按钮：全屏、左半、右半、上半、下半、中心等
  - 参考文件独立缩放控制（10%-500%）
- **视频模板时间线** — 框选视频模板时显示时间线进度条，可拖动查看不同时间段
- **模板预览功能** — "预览模板"按钮支持缩放、旋转、视频播放
- **区域/装饰复选框** — 左/中/右区域和装饰视频均可独立开关
- **装饰视频框选** — 选择装饰视频后自动弹出区域框选，不框选则不添加
- **片头片尾独立模式** — 新增独立"片头片尾"标签页，可单独添加片头/片尾视频
  - 支持仅片头、仅片尾、同时添加
  - 复选框控制是否启用
  - 使用 `-c copy` 快速拼接，不重新编码

#### 分批次输出
- **按集数分割文件夹** — 每个文件夹内部按集数分割
  - 例：26集按11集分割 → 输出_P1(1-11集)、_P2(12-22集)、_P3(23-26集)
  - 所有文件直接输出到选定目录，不创建子文件夹

### 🔧 优化改进

- **NVENC时长修复** — 彻底解决NVENC编码器不遵守 `-t` 输出限制的问题
  - 方案：预裁剪模板视频到正片时长（stream copy，瞬间完成）
  - 裁剪结果缓存到 `.tpl_cache/`，相同片长不重复裁剪
- **临时文件管理** — 改用 `.temp/` 子目录，处理完自动清理
  - 每个文件夹处理完立刻清理，不等全部完成
  - 停止/完成时自动清理残留临时文件
- **UI优化**
  - 删除spinner旋转加载器，界面更简洁
  - "模板图片"改为"模板"
  - 装饰视频选中后显示绿色高亮边框

### 🐛 Bug修复

- 修复图片模板预编译失败时 `-loop 1` 导致时长异常（23小时）
- 修复 `-stream_loop -1 -t X -i` 组合导致输出时长2.3倍
- 修复CTkLabel不支持 `highlightthickness` 导致崩溃
- 修复 `import hashlib` 缺失导致裁剪功能报错
- 修复分段处理路径中 `-t` 在输入端的时长异常

---

## [2.1.0] - 2026-06-07

### 🆕 新增功能

#### Web端
- **流水线模式** — 串联多个处理步骤（套框→画中画→转码等），自动顺序执行
- **合并视频** — 多个视频文件合并为一个，支持自动排序和音视频同步
- **布局可视化预览** — 套框/画中画页面新增"预览布局"按钮，弹窗显示模板+区域叠加效果
  - 套框模式：绿色半透明区域 + 边框 + 尺寸标注
  - 画中画模式：蓝/绿/红三色区分 left/center/right 三个区域
- **键盘快捷键扩展** — 1-6键快速切换模式（套框/画中画/横转竖/格式转换/流水线/合并）
- **合并模式文件选择** — 支持拖拽/选择多个视频文件，显示文件列表和总大小

#### GUI端
- **布局可视化预览** — 套框/画中画页面新增"预览"按钮，功能与Web端一致
- **CTk全面UI重构** — 所有组件统一使用CustomTkinter圆角组件+深色主题
  - 模式选择条、设置卡片、状态指示器、命令预览等全部CTk化
  - 按钮统一使用 `_btn()` 工具方法，支持5种颜色主题

### 🔧 优化改进

- **窗口比例优化** — 窗口占屏幕92%x88%，间距全面压缩，同屏显示更多内容
- **视频列表固定** — 切换模式时视频列表不再重建，避免闪烁
- **左栏可滚动** — 模式内容区支持鼠标滚轮滚动，小屏幕不截断内容
- **滚动事件优化** — 鼠标离开右栏时自动解绑滚轮，不再全局干扰
- **中文路径兼容** — subprocess加 `errors="replace"`，FFmpeg输出含中文不再崩溃
- **CTk按钮兼容** — 39处 `.config()` → `.configure()`，修复CTk控件不支持config的问题
- **Checkbox修复** — 7个 `tk.Checkbutton` → `CTkCheckBox`，深色主题下可见可点击

### 🐛 Bug修复

- 修复 `CTkButton` 不支持 `variable`/`value` 参数导致启动崩溃
- 修复模式按钮无法选中点击的问题
- 修复分段处理/分批次输出等Checkbox不可见的问题
- 修复按钮 `.config()` 在CTk控件上抛 `AttributeError`
- 修复Windows GBK编码导致FFmpeg输出解码崩溃
- 修复Web端键盘快捷键 `mode[]` 未定义错误

---

## [2.0.0] - 2026-05

### 初始版本
- Web端视频处理工具（Flask + HTML/CSS/JS）
- GUI端视频处理工具（CustomTkinter）
- 支持4种模式：套框、画中画、横转竖、格式转换
- 编码预设（B站推荐/高质量/中等/快速）
- 实时命令预览
- NVENC/软件编码支持
