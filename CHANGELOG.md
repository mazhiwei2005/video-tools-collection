# 更新日志 / Changelog

## [2.1.0] - 2026-06-07

### 🆕 新增功能

#### Web端
- **流水线模式** — 串联多个处理步骤（套框→画中画→转码等），自动顺序执行
- **合并视频** — 多个视频文件合并为一个，支持自动排序和音视频同步
- **布局可视化预览** — 套框/画中画页面新增"预览布局"按钮，弹窗显示模板+区域叠加效果
  - 套框模式：绿色半透明区域 + 边框 + 尺寸标注
  - 画中画模式：蓝/绿/红三色区分 left/center/right 三个区域
- **键盘快捷键扩展** — 1-6键快速切换模式（套框/画中画/横转竖/格式转换/流水线/合并）
- **合并模式文件选择** — 支持拖拽/选择多个视频文件，显示文件列表和总大小

#### GUI端
- **布局可视化预览** — 套框/画中画页面新增"预览"按钮，功能与Web端一致
- **CTk全面UI重构** — 所有组件统一使用CustomTkinter圆角组件+深色主题
  - 模式选择条、设置卡片、状态指示器、命令预览等全部CTk化
  - 按钮统一使用 `_btn()` 工具方法，支持5种颜色主题

### 🔧 优化改进

- **窗口比例优化** — 窗口占屏幕92%x88%，间距全面压缩，同屏显示更多内容
- **视频列表固定** — 切换模式时视频列表不再重建，避免闪烁
- **左栏可滚动** — 模式内容区支持鼠标滚轮滚动，小屏幕不截断内容
- **滚动事件优化** — 鼠标离开右栏时自动解绑滚轮，不再全局干扰
- **中文路径兼容** — subprocess加 `errors="replace"`，FFmpeg输出含中文不再崩溃
- **CTk按钮兼容** — 39处 `.config()` → `.configure()`，修复CTk控件不支持config的问题
- **Checkbox修复** — 7个 `tk.Checkbutton` → `CTkCheckBox`，深色主题下可见可点击

### 🐛 Bug修复

- 修复 `CTkButton` 不支持 `variable`/`value` 参数导致启动崩溃
- 修复模式按钮无法选中点击的问题
- 修复分段处理/分批次输出等Checkbox不可见的问题
- 修复按钮 `.config()` 在CTk控件上抛 `AttributeError`
- 修复Windows GBK编码导致FFmpeg输出解码崩溃
- 修复Web端键盘快捷键 `mode[]` 未定义错误

---

## [2.0.0] - 2026-05

### 初始版本
- Web端视频处理工具（Flask + HTML/CSS/JS）
- GUI端视频处理工具（CustomTkinter）
- 支持4种模式：套框、画中画、横转竖、格式转换
- 编码预设（B站推荐/高质量/中等/快速）
- 实时命令预览
- NVENC/软件编码支持
